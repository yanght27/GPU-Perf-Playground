# Txx <名称>（唯一主讲义模板）

> 使用说明：每个 Ticket 只维护一份 `docs/lectures/Txx-<name>.md`。它是对话讲解的**完整留存**，
> 不是代码注释的搬运，也不是摘要。头部两个导读 bullet 是“30 秒预览”，长度与格式
> 必须与其他 Ticket 一致（四段式/五段式，见下）。

- Ticket：Txx
- 状态：`学习中` / `已过关`
- 唯一学习变量：<一句话>
- 环境：<env / 容器>
- 官方来源：Sxx, Sxx（对应 `config/source-ledger.md`）
- 跨 Ticket 术语：`docs/CONCEPTS.md`
- 本节导读：**一句话目标**——<要达成的能力>；**依次学到**——①…②…③…；**学完应能回答**——<2-3 个问题>；**相关工具/技术**——<工具/技术清单>。
- 本节内容：**要解决的问题**——<承接上一 Ticket 留下的问题>；**核心手段**——<本节关键技术，每个用“什么是什么、为什么有用”一句话>；**怎么实现**——<最小实现路径，如哪个文件/哪个变体>；**怎么验证**——<正确性 + 工具证据>；**最终结论**——<本节最该记住的判断>。

## 1. 上一轮问题回答（如有）

## 2. 规范实现与官方来源

- 每个关键 API/命令/实现模式 → 台账编号 → 官方文件路径（可摘录关键片段）。

## 3. 本轮实现结果

- 实际运行的命令与关键输出（正确性、benchmark 摘要）。
- 证据文件清单：`docs/evidence/Txx/...`。

## 4. 核心代码与逐行解释

### 4.0 零基础先修：概念地图

- 框架/工具类 Ticket 必须先写这一小节，把“这个工具/框架是什么、为什么需要它、核心名词是什么”讲清，再进入代码。
- 例如：Transpose 是什么、Reduction 是什么、Qwen 是什么、vLLM 是什么、ModelScope 快照是什么。
- 这一节放在“核心代码与逐行解释”里，不单独成章。

### 4.1 核心代码（编号按实际 Ticket 调整）

- 本轮新增/修改的最小核心代码（可多路径并列，但只放与唯一学习变量相关的部分）。
- 每个代码块之后逐行/逐段解释，不跳行。

## 5. 核心知识点要点讲解

### 5.x 工具/框架定位与命令行（框架/工具类 Ticket T19–T28 必写）

- **这个工具/框架是什么**：解决什么问题、抽象层级、典型使用场景。
- **和相邻工具/框架的区别**：例如 vLLM vs SGLang vs TensorRT-LLM vs ms-swift、Transformers vs vLLM、PyTorch vs DeepSpeed vs ms-swift。
- **核心命令行逐条讲解**：每条命令是什么、为什么这样写、输出怎么看、常见参数。
- **官方来源编号**：每个命令/API 对应 `config/source-ledger.md` 中的 Sxx。

### 5.y 其他知识点

- 只讲本 Ticket 的知识点；由浅入深；理论推导与实测结论分开标注。

## 6. 性能分析

- 实测数据表 + 解读；warmup/同步/计时方法写明；不写预测值。

## 7. Memory-Bound / Compute-Bound / Latency-Bound 判断

- 结论 + 工具证据（NCU/NSYS/SASS 指标）；证据不足就明写“结论为假设”。

## 8. 知识点完整性检查

- 对照 `config/coverage-matrix.md`：本轮覆盖了哪些行；未覆盖项归哪个后续 Ticket 或为何受限。

## 9. 过关问题及答案（N 题，一问一答）

- 2–4 个以上，聚焦唯一学习变量，分层（基础/理解/应用）。
- **一问一答紧邻**：`**Q1.** 题目` 之后立刻 `**A1（回答）**：答案`，禁止把题目和答案
  分成两节。答案可以短，但必须完整通顺、讲清“为什么”，让零基础学习者读得懂。

## 10. 本轮停止点

- 做到哪里；哪些是理论推导 / 实测 / 受环境阻塞。

## 11. 下一最小增量

- 只写标题和一句为什么，不写实现。

## 附录：可复现原生命令

- NSYS / NCU / SASS 等原生命令与输出文件路径；便捷脚本可以给，但原生命令必须展开。
